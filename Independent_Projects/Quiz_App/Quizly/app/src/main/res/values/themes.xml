<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Quizly" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimaryButtonBg</item>
    </style>

    <style name="Base.Theme.Quizly.DarkTheme" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimaryButtonBg</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="Theme.Quizly" parent="Base.Theme.Quizly" />
</resources>